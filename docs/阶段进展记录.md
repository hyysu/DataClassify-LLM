# 阶段进展记录

## 当前阶段

项目已完成“本地可运行 MVP + 分类评估闭环”。

当前不依赖大模型 API key，也不依赖 Ollama。

## 已完成能力

1. 字段元数据 CSV 输入。
2. 字段标签体系维护。
3. 贝叶斯字段分类模型训练。
4. 关键词和正则线索加权纠偏。
5. 本地规则分级。
6. CSV/JSON 分类分级报告导出。
7. 分类模型评估报告导出。
8. 自动化测试。
9. Gradio 本地页面。

## 当前评估结果

基于当前示例样本集：

```text
Samples: 115
Train: 69
Test: 46
Accuracy: 1.0
Macro-F1: 1.0
```

说明：

该结果只代表当前原型样本集上的表现，不代表真实业务数据上的最终效果。后续接入真实字段后，应重新运行评估脚本，并重点查看错分样本。

## 关键文件

- 标签体系：`data/field_label_catalog.csv`
- 训练样本：`data/training_fields.csv`
- 示例输入：`data/demo_fields.csv`
- 分类器：`src/data_classification_tool/classifier.py`
- 分级器：`src/data_classification_tool/grader.py`
- 评估模块：`src/data_classification_tool/evaluator.py`
- 命令行入口：`src/data_classification_tool/cli.py`

## 运行命令

训练模型：

```powershell
& "D:\conda\envs\data-classification\python.exe" D:\classification\data-classification-tool\scripts\train_model.py
```

分析示例字段：

```powershell
& "D:\conda\envs\data-classification\python.exe" D:\classification\data-classification-tool\scripts\analyze_demo.py
```

评估模型：

```powershell
& "D:\conda\envs\data-classification\python.exe" D:\classification\data-classification-tool\scripts\evaluate_model.py
```

运行测试：

```powershell
& "D:\conda\envs\data-classification\python.exe" -m pytest D:\classification\data-classification-tool\tests
```

启动页面：

```powershell
& "D:\conda\envs\data-classification\python.exe" D:\classification\data-classification-tool\scripts\run_gradio.py
```

访问地址：

```text
http://127.0.0.1:7860
```

## 当前产物

- 模型文件：`models/field_classifier.joblib`
- 示例 CSV 报告：`reports/demo_report.csv`
- 示例 JSON 报告：`reports/demo_report.json`
- 评估指标：`reports/evaluation/evaluation_metrics.json`
- 评估明细：`reports/evaluation/evaluation_predictions.csv`
- 混淆矩阵：`reports/evaluation/confusion_matrix.csv`
- Excel 评估报告：`reports/evaluation/evaluation_report.xlsx`
- 页面报告目录：`reports/ui`

## 下一步建议

1. 用真实业务字段扩充 `training_fields.csv`。
2. 每个标签至少准备 20 条样本。
3. 增加人工复核字段和复核结果回流机制。
4. 增强页面，增加人工复核字段和复核结果回流。
5. 后续有 API key 或本地模型后，再接入 LLM 分级。
